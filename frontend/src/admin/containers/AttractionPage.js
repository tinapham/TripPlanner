import React from 'react';
import AttractionManagement from "../attractions/index";

const AttractionPage = (props) => {
  return (
    <AttractionManagement  data={props}/>
  );
};
export default AttractionPage;
